Backburner:

# font - it's not rendering, maybe the path is incorrect

Notes:

# RouteFactory is like if, else
# The screens are stacks. Homepage (recipe) is top layer 
# and recipe detail is a stack that goes on top but homepage is saved